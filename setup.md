```shell
# Voltaのインストール
$ winget install Volta.Volta
```

```shell
# Node.jsのインストール
$ volta install node
```

```shell
# Androidスタジオのインストール
https://developer.android.com/studio?hl=ja

# 仮想デバイスの作成（デフォルトでも可）
https://developer.android.com/studio/run/managing-avds?hl=ja

# platform-toolsのpathを通す
C:\Users\<YourUsername>\AppData\Local\Android\Sdk\platform-tools
$ adb --version

# emulatorのpathを通す
C:\Users\<YourUsername>\AppData\Local\Android\Sdk\emulator
$ emulator --version

# android virtual deviceのリストを表示
$ emulator -list-avds

# エミュレータ起動
$ emulator -avd <AVD名>
```

```shell
# プロジェクトのクローン
# C:\User\dev or C:\User\dev_collab 直下にクローン推奨
$ git clone https://github.com/JauneBrillant/todo.git todo
```

```shell
# 依存パッケージのインストール
$ npm install
```

```shell
# 開発サーバーの起動
$ npx expo start
```
